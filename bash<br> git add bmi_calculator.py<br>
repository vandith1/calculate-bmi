python<br># BMI Calculator Application in Python<br><br># Required Libraries<br>import sys<br><br>def calculate_bmi(weight, height):<br> Calculate BMI based on weight and height. bmi = weight / (height ** 2)<br> return bmi<br><br>def get_bmi_category(bmi):<br> Determine BMI category. if bmi < 18.5:<br> return Underweight elif 18.5 <= bmi < 24.9:<br> return Normal weight elif 25 <= bmi < 29.9:<br> return Overweight else:<br> return Obesitydef main():<br> print(Welcome to the BMI Calculator! <br> # Input weight<br> weight = float(input(Enter your weight (in kg or lbs): weight_unit = input(Is this in kg or lbs? (Enter 'kg' or 'lbs'): strip().lower()<br> <br> # Input height<br> height = float(input(Enter your height (in meters or inches): height_unit = input(Is this in meters or inches? (Enter 'm' or 'in'): strip().lower()<br> <br> # Convert weight to kg if in lbs<br> if weight_unit == 'lbs':<br> weight *= 0.453592 # Convert lbs to kg<br> <br> # Convert height to meters if in inches<br> if height_unit == 'in':<br> height *= 0.0254 # Convert inches to meters<br> <br> # Calculate BMI<br> bmi = calculate_bmi(weight, height)<br> category = get_bmi_category(bmi)<br> <br> print(fYour BMI is: {bmi:.2f} print(f are classified as: {category}if __name__ == main__ main()<br>
